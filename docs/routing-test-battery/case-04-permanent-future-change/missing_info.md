# Missing info

The change is clear enough to open a revision; the revision form fills the rest.
Please fill the workflow revision request
(`methodology/templates/workflow-revision-request.md`).

To proceed I need:
- What is wrong or missing today that prompts the change - why the team-wide,
  full-length version is not working for the execs.
- A short example or sketch of the half-page exec version so we know what good
  looks like after the change.

The workflow (weekly-status-summary), the desired change (exec audience, half a
page), and the change type (Audience plus Output format) are already clear.
