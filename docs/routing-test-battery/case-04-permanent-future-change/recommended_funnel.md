C

This concerns `weekly-status-summary`, which is `status: production` in
`portfolio/workflows.yaml`, so Q1 is yes. The requester explicitly wants the
audience and the length changed for every future run - "permanent," "every week
from now on" - which is a change to future behavior, so Q2 is yes. That is a
Funnel C revision: a new audience (the exec list) and a tighter format are
exactly the kinds of change the contract governs. Open it contract-first with
`/revise-workflow weekly-status-summary`; nothing about this is a one-time note.
