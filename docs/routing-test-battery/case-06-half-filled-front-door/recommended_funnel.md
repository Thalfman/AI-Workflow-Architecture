A

No production workflow covers a monthly vendor scorecard - `weekly-status-summary`
is the only entry in `portfolio/workflows.yaml` and it is unrelated - so Q1 is no
with no non-production match to resume. The visible answers settle the routing
question on their own: question 1 describes a clear deliverable and question 3
confirms it recurs monthly, so Q4 is yes and this is a new-workflow intake. The
two blank questions ("have we done this before," "by when") do not change the
funnel; routing classifies on what is present. Hand it to `/scope`, which applies
its own reject/defer/merge/one-off/create judgment.
