# Missing info

This routed to C because of the permanent Risks Heatmap change, but the request
also asks for this week's run - handle both. To open the revision, please fill
the workflow revision request
(`methodology/templates/workflow-revision-request.md`).

To proceed I need:
- What is wrong or missing today that the Risks Heatmap fixes (why risks are hard
  to see in the current format).
- The kind of change it is (this looks like Output format plus Content rules).
- A short example or sketch of the heatmap so we know what good looks like.

For the this-week run, do not wait on the revision. Either:
- Split it now: run this week's summary under the current contract with
  `/run-workflow weekly-status-summary <input-folder>`, then revise; or
- Queue a Funnel B run for the heatmap-enabled summary once the revision lands.

To run this week now I also need the three contributor updates attached as the
run inputs.
