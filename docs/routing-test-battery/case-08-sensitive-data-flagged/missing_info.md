# Missing info

Please provide the run inputs via the existing-run form
(`methodology/templates/workflow-run-input.md`).

To proceed I need:
- The contributor updates for this run - but given the confidential customer
  detail, do not paste them into any committed file. Provide them through the
  local, gitignored run-input channel so raw content stays out of the repo.
- The exact week this run covers.

Deadline (end of day Friday) is clear, and the sensitivity is already flagged as
confidential - which is exactly why the raw inputs are handled locally per
`methodology/methods/sensitivity-policy.md`.
