D

No production workflow covers a board memo - `weekly-status-summary` is the only
workflow in `portfolio/workflows.yaml` and it is unrelated - so Q1 is no and
there is no non-production workflow to resume. The requester is explicit that
this is a one-off tied to a single board meeting and will not repeat, so Q4 is
no. That makes it a Funnel D one-off assist: valuable to do once, not worth a
standing workflow. Do it directly; do not create a workflow. If board memos turn
out to recur, it becomes a new-workflow intake later.
