# Final output — this run

The delivered output, after review and any required edits. This is what the operator
sends to the requester.

**Sensitivity:** if this workflow's `max_sensitivity` is `internal` or higher, save
this file as `FINAL_OUTPUT.local.md` so the raw content is gitignored. The committed
evidence of the run is the hashes and sign-off in `RUN_RECORD.md`, not this text
(`methodology/methods/sensitivity-policy.md`).

---

_(paste the delivered output here)_
