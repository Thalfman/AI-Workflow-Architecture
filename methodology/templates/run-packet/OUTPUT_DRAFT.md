# Output draft — this run

The first generated output, before review. Produced by running `PROMPT_USED.md` over
this run's inputs (operator + Claude — no script ever generates the output).

**Sensitivity:** if this workflow's `max_sensitivity` is `internal` or higher, save
this file as `OUTPUT_DRAFT.local.md` so the raw content is gitignored and never
committed. The committed evidence of the run is the hashes in `RUN_RECORD.md`, not
this text (`methodology/methods/sensitivity-policy.md`).

---

_(paste the generated draft here)_
