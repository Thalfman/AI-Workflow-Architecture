---
description: Scope an intake folder and produce a reply, draft contract, and recommendation.
argument-hint: <path-to-intake-folder>
---

Execute the specification in @methodology/commands/scope.md, treating `$ARGUMENTS` as its inputs.

Obey the rules in the root `CLAUDE.md` and `methodology/CLAUDE.md`. The methodology spec is
the single source of truth for this command — do not deviate from it. This file is only a
pointer so the command is discoverable in Claude Code; never duplicate the spec's content here.
